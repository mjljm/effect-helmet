import * as DV from './directive-values.js';

/**
 * @since 1.0.0
 * @category models
 */

export type UpgradeInsecureRequestDirectiveValue = DV.DirectiveValue<never>;

/**
 * Creates an empty UpgradeInsecureRequestDirectiveValue
 * Remark: 'none' not allowed for this directive'
 * @since 1.0.0
 * @category constructors
 */
export const empty = DV.empty<never>();
